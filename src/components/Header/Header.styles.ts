import { styled, theme } from "@/src/stitches";

export const Wordmark = styled("h1", {
  color: theme.colors.salsa,
  fontWeight: 900,
  fontStyle: "italic",
  fontSize: "2.5rem",
  padding: "$large",
});
